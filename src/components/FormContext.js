import React, { createContext, useState } from 'react';

export const FormContext = createContext(null);
export const ProfileUpdated = createContext(false);
export const AudienceContext = createContext(null);

